conda create -n vae-project python=3.12 -y
conda activate vae-project

conda install -c nvidia cuda-runtime=12.4 -y
pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu124

pip install lightning hydra-core wandb clean-fid matplotlib

python -c "import torch; print(f'CUDA: {torch.version.cuda}'); print(f'GPU: {torch.cuda.get_device_name(0)}' if torch.cuda.is_available() else 'FAIL')"

conda env export --no-builds | grep -v "^prefix: " > environment.yml